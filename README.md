# Streaming_JSON

We wanted to make a webscraping script that was easy enough to modify to adapt it to each website we where going to scrape. We managed to gather, the title, the actual link to the video and the description. All of the data gathered are then writing in a JSON file.
